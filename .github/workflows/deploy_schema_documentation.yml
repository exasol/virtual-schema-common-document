name: Deploy Schema Documentation
on:
  push:
    branches:
      - master
jobs:
  create-and-deploy-schema-doc:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v2.3.1
        with:
          persist-credentials: false

      - name: Install and Build
        run: |
          sudo npm install -g bootprint bootprint-json-schema
          mkdir doc/gh_pages/schema_doc
          bootprint json-schema src/main/resources/schemas/edml-1.0.0.json doc/gh_pages/schema_doc/edml_1.0.0/
          bootprint json-schema src/main/resources/schemas/edml-1.1.0.json doc/gh_pages/schema_doc/edml_1.1.0/
          bootprint json-schema src/main/resources/schemas/edml-1.2.0.json doc/gh_pages/schema_doc/edml_1.2.0/
          cp -r doc/gh_pages/schema_doc/edml_1.2.0/ doc/gh_pages/schema_doc/edml_latest/

      - name: Install SSH Client 🔑
        uses: webfactory/ssh-agent@v0.4.1
        with:
          ssh-private-key: ${{ secrets.DEPLOY_KEY }}

      - name: Deploy 🚀
        uses: JamesIves/github-pages-deploy-action@3.7.1
        with:
          ssh: true
          BRANCH: gh-pages
          FOLDER: doc/gh_pages/
          SINGLE_COMMIT: true
          CLEAN: true